<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inventory">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/ic_inventario"
        android:label="Conteo físico de inventario"
        android:largeHeap="true"
        android:roundIcon="@drawable/ic_inventario"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".user_details"></activity>
        <activity android:name=".UserView" />
        <activity android:name=".inventoryDetails" />
        <activity android:name=".inventoryView" />
        <activity android:name=".Art_details" />
        <activity android:name=".View_art" />
        <activity android:name=".Create_art" />
        <activity android:name=".createUser" />
        <activity
            android:name=".DisplayMessageActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>